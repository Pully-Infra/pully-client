{
  "name": "pully-client",
  "version": "1.0.31",
  "description": "Client for pully realtime infrastructure",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsup",
    "esbuild": "esbuild src/index.ts --bundle --outfile=dist-esbuild/index.js",
    "publish": "npm run build && npm publish"
  },
  "author": "David Ajayi",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^20.7.1",
    "socket.io-client": "^4.7.2",
    "typescript": "^5.2.2"
  },
  "devDependencies": {
    "tsup": "^7.2.0"
  }
}